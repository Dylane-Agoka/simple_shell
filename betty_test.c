#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints "I'm passed the Betty checks"
 * Return: Always 0 (Success)
 */

int main(void)
{
printf("I'm passed the Betty checks\n");

return (0);
}
